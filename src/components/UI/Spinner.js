import Modal from './Modal';
import spinnerImage from '../images/spinner.gif';
const Spinner=()=>{
    return<Modal>
      <img src={spinnerImage} alt=''></img>
    </Modal>
}

export default Spinner;